#include <iostream.h>
#include <math.h>

void main()
{
int a,b ;
cout<<"Enter the year which you want to check if leap or not.";
cin>>a;
b=a%4==0 ;
if (a%4==0)
{ cout<<"This is a leap year" ;
}
else
{ cout<<"This isn't a leap year" ;
}}

